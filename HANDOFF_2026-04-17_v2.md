# HANDOFF — live (최종 업데이트 2026-04-19)

새 Claude/GPT 세션이 이 repo 를 맡게 되면 **이 문서 + `CLAUDE.md` 두 개만 읽고도 이어서 일할 수 있다.**
이 파일은 세션이 바뀔 때마다 in-place 로 덮어써서 최신 상태를 유지한다 (새 파일 만들지 말 것).

---

## 0. 한 줄 요약

> Pack Chain (Phase 1 + 1.1) 구현 완료, 푸시됨. **라이브 검증 대기 중.**
> `pack_chain_enabled=True` 로 활성화 → `deploy-x <브랜치>` → 대시보드에서 초안 1건 돌려서 sidecar / Grok 편집카드 / 로그 확인.

---

## 1. 현재 브랜치 / HEAD

| 항목 | 값 |
|---|---|
| 브랜치 | `claude/review-handoff-document-UoyuK` |
| 최신 HEAD | `a880688` — Phase 1.1 구조 강화 |
| 직전 커밋 | `e567510` — Phase 1 Pack Chain 도입 |
| 원격 푸시 | ✅ `origin/claude/review-handoff-document-UoyuK` 동기화 완료 |
| 서버 반영 | ⏳ **아직 deploy-x 안 돌림** (사용자가 직접 돌려야 함) |

---

## 2. 지금까지 해둔 일 (이 세션 스택)

### Phase 1 — Pack Chain 도입 (`e567510`)
- 신규 모듈 3개 추가:
  - `app/services/source_pack.py` — Research + FactCheck + 원문 → 재료 dict
  - `app/services/angle_pack.py` — source_pack → 각도 dict (Gemini 1회 or heuristic)
  - `app/services/grok_handoff.py` — compose_enriched_source / compose_pack_context / format_handoff
- `app/orchestrator.py` 에 Pack Chain 경로 삽입 (feature-flag: `pack_chain_enabled`, 기본 False)
  - 성공: 기존 DraftWriter/Reviewer 에 pack_context / enriched_source 주입
  - 실패: `[PACK_CHAIN_FALLBACK]` 로그 + `_legacy_generate_and_save` 로 안전 우회
- Sidecar JSON: `/root/x-posting-system/runtime_x/packs/{draft_id}.json` (atomic tempfile + os.replace)
- 텔레그램 UI: 카드 하단에 "🧷 Grok copy" 버튼 추가 → `format_handoff` 결과 복사
- 라이브 검증 **완료**: draft 1367 에서 sidecar 정상 생성, fallback 로그 없음 확인

### Phase 1.1 — 구조 강화 (`a880688`, 방금 끝남)
잘 읽히는 글을 만들기 위한 4 축을 Pack Chain 에 주입. **Phase 1 전부 그대로 유지.**

- `source_pack.py` — 4 key 추가 (6 → 10):
  - `historical_parallel` — 과거-현재 비교 문장 (unlike / 2017 / previously 등 힌트 매칭)
  - `concept_translation` — 어려운 개념 한 줄 풀이 (fact_labels = missing_context 우선)
  - `evidence_pack` — 숫자/기관/인용 ≤5개, 각 ≤240자. **원문 매칭만, hallucination 금지**
  - `closing_signal` — 마지막 문장용 포인트 1줄
- `angle_pack.py` — 5 key 추가 (6 → 11), **Gemini 호출수 1회 유지**:
  - `frame_type` enum — parallel / contrast / hidden_signal / underreported_angle
  - `story_spine` list — ["hook","explain","evidence","contrast","close"] 기본값
  - `readability_risk` enum — low / medium / high
  - `share_trigger` — 공유 동기 한 줄
  - `scan_pattern` — 모바일 스캔 패턴 힌트
- `grok_handoff.py` — `format_handoff` 를 **"편집 입력 카드"** 로 재작성:
  - ≤1800자 (기존 3500 → 1800)
  - 고정 5블록: `## 원문 초안 / 이 글의 핵심 각도 / 절대 바꾸지 말 것 / 바꿔도 되는 것 / 최종 출력 규칙`
  - 선택 2블록: `## 어려운 개념 한 줄 번역 / 핵심 근거 2~3개` (데이터 있을 때만)
  - URL 정규식으로 완전 제거, 본문 예산은 고정 5블록 보존 이후 남은 공간
- `orchestrator.py` — 로그 1줄만 수정:
  ```
  [pack_chain] active — winner=… frame=… spine=a/b/c risk=…
  ```
- `tests/test_pack_chain_structure.py` 신규 25 케이스 — 구조 키 / enum / cap / hallucination / URL 덤프 금지 / 편집 카드 블록 / compose 역호환 / 플래그 기본값 off

**테스트 결과**: Phase 1.1 로컬 25/25 pass. 전체 1350 pass, 4 fail (전부 base branch 사전존재 — test_config×3 + test_e2e×1, Phase 1.1 과 무관).

---

## 3. Pack Chain 데이터 흐름 (필수 암기)

```
SourceItem + Research + FactCheck
        │
        ▼
  source_pack  (10 key dict)
        │
        ▼
  angle_pack   (11 key dict, Gemini 1회)
        │
   ┌────┴────────────┐
   ▼                 ▼
compose_            compose_
enriched_source     pack_context
   │                 │
   ▼                 ▼
DraftWriter(source_text=…) + Reviewer(criteria_context+=…)
   │
   ▼
final_body
   │
   ▼
format_handoff(source_pack, angle_pack, final_body)   ──▶  sidecar JSON + Telegram Grok 버튼
```

**활성 조건**: `settings.pack_chain_enabled == True` (기본값 False).
**실패 안전장치**: 어떤 단계에서 raise 되든 `[PACK_CHAIN_FALLBACK] {ErrType}: {msg}` 로그 남기고 legacy 경로 그대로 돔.

---

## 4. 라이브 검증 체크리스트 (다음 세션이 **처음 할 일**)

1. **플래그 켜기**
   ```bash
   ssh root@107.191.61.190
   grep pack_chain_enabled /root/x-posting-system/.env || echo 'PACK_CHAIN_ENABLED=true' >> /root/x-posting-system/.env
   ```
2. **배포**
   ```bash
   deploy-x claude/review-handoff-document-UoyuK
   ```
3. **로그 스트림 열고**
   ```bash
   journalctl -u x-posting-bot -f | grep -E "pack_chain|PACK_CHAIN_FALLBACK"
   ```
4. **대시보드에서 초안 1건** 생성 → 기대 로그:
   ```
   [pack_chain] active — winner=… frame=… spine=hook/explain/evidence/contrast/close risk=…
   ```
   `[PACK_CHAIN_FALLBACK]` 가 찍히면 **실패** — 로그 전문 복사해서 보고.
5. **sidecar 확인**
   ```bash
   ls -la /root/x-posting-system/runtime_x/packs/ | tail
   python -m json.tool /root/x-posting-system/runtime_x/packs/<draft_id>.json | head -60
   ```
   - `historical_parallel`, `concept_translation`, `evidence_pack`, `closing_signal` 4개 필드가 모두 존재해야 함 (빈 값이어도 key 는 있어야 함)
   - `angle_pack.frame_type`, `story_spine`, `readability_risk`, `share_trigger`, `scan_pattern` 5개 필드 동일
6. **텔레그램 Grok copy 버튼** 눌러보고 결과 점검:
   - ≤1800자
   - URL 없음 (http / https 둘 다)
   - `## 원문 초안 / 이 글의 핵심 각도 / 절대 바꾸지 말 것 / 바꿔도 되는 것 / 최종 출력 규칙` 5개 헤딩 모두 존재
   - 선택 2블록은 데이터 있을 때만 (없으면 없어도 됨)

---

## 5. 서버 / 배포

| 항목 | 값 |
|---|---|
| IP | 107.191.61.190 |
| 서비스 | systemd `x-posting-bot` |
| DB | `/root/x-posting-system/x_poster.db` |
| venv | `/root/x-posting-system/venv/bin/python` |
| 대시보드 | http://107.191.61.190:8000/control/ |
| sidecar 저장소 | `/root/x-posting-system/runtime_x/packs/` |

### 배포 표준 (단 하나의 공식 명령)
```bash
deploy-x claude/review-handoff-document-UoyuK
# 또는
bash /root/x-posting-system/scripts/deploy.sh claude/review-handoff-document-UoyuK
```

**절대 하지 말 것** (CLAUDE.md 참조):
- 서버에서 직접 `python run.py` 수동 실행 금지 → Conflict 100%
- `xdashboard.service` enable 금지 → 중복 polling
- `systemctl restart x-posting-bot` 단독 실행 금지 → deploy.sh 전체 흐름 타야 함

---

## 6. 작업 방식 (이어받는 세션이 지킬 룰)

1. **먼저 이 파일 + CLAUDE.md 읽기** — 배포 표준 / 금지사항 / 아키텍처 요점
2. **최소 수정 원칙** — broad refactor 금지, 모델 역할 재배치 금지
3. **승인형 흐름 유지** — 슬롯 선택 → 마감 플로우 건드리지 말 것
4. **건드리지 말 것**:
   - top5_briefing / DB 스키마 / 공급자 인터페이스
   - `content_pack.py` 대대적 확장
   - 대시보드 구조
   - X 자동 게시 부활
5. **Pack 확장은 기존 파일 안에서** — 새 Pack 파일 만들지 말 것 (source_pack / angle_pack / grok_handoff 안에서만 키 추가)
6. **테스트**: `pytest tests/test_pack_chain_structure.py` (25 pass 유지) + `pytest tests/test_content_pack.py`
7. **커밋**: 한글 + `https://claude.ai/code/session_...` 포함
8. **배포 후 검증**: 위 섹션 4 체크리스트 전부 통과해야 완료
9. **대화는 휴대폰에서 명령어 복붙으로 진행** — 명령은 한 줄로 합쳐서 제시
10. **보고서는 코드블록** — 사용자가 GPT 로 옮길 때 그대로 복붙하도록

---

## 7. 사용자가 원하는 흐름 (세션 사이 continuity)

> **현재 스택**: Phase 1.1 구현 끝 → **push 끝** → **라이브 검증 대기** → 통과하면 Phase 2 로 넘어감

- **Phase 2 범위 (아직 착수 안함)**: 사용자와 먼저 논의 후 스펙 확정. 라이브 검증 통과 전엔 시작 금지.
- **검증 실패 시**: Phase 1.1 롤백 없이 fallback 경로가 자동으로 돌고 있을 것. `[PACK_CHAIN_FALLBACK]` 로그 원문 보고하면 세션이 수정.

---

## 8. 아키텍처 요점 (라우터 v1 — 유지)

CLAUDE.md 에 적혀있지만 여기에도 복기:
- 5 AI 역할 분리: Perplexity(factcheck) / Gemini(slots + anglepack) / OpenAI(finalize) / Grok(X sense 평가 + 편집) / Claude(편집 + 리뷰)
- 기사 라우터 (`app/services/content_pack.py`):
  - `route_article_mode(card)` → `EXPLAIN` / `JUDGMENT` / `VERIFY`
  - 텔레그램 UI: 📊 EXPLAIN / ⚖️ JUDGMENT / 🔎 VERIFY
- 최종 마감 타임아웃: 180초 (`telegram_bot.py:1680`)
- 카드 문장 ≠ 게시 문장 (완전 분리)
- VERIFY 모드는 확정 어휘 금지 ("본심" / "노림수" / "체제 양보" 등)

---

## 9. API 엔드포인트 (변동 없음)

| Endpoint | 용도 |
|---|---|
| POST /ingest | 초안 생성 |
| POST /drafts/{id}/approve | 초안 승인 + Grok mini-pass |
| POST /drafts/{id}/reject | 초안 거절 |
| GET /control/status | 시스템 상태 |
| GET /control/editorial-summary | 점수 + 큐 |
| GET /control/providers | AI 역할 현황 |
| GET /control/flow-trace | 초안 리스트 |
| GET /control/recent-news | 최근 기사 |
| GET /control/scored-candidates | 점수 통과 기사 (AI 비용 0) |
| GET /control/naver | Naver API 사용량 |
| GET /control/business-summary | premium/b2b/newsletter |

---

## 10. 이전 핸드오프 (읽을 필요 없음, 참고용)

- `HANDOFF_2026-04-11.md` — 대시보드 리디자인 이전 스냅샷
- `HANDOFF_2026-04-12.md` — 대시보드 중간 상태
- `HANDOFF_2026-04-17.md` — 대시보드 리디자인 직후
- **이 파일 (HANDOFF_2026-04-17_v2.md)** — **현재 라이브 (Pack Chain Phase 1 + 1.1)**. 세션 바뀔 때마다 이 파일을 덮어써서 최신화.

---

## 11. 마지막 한 줄

> **다음 세션은 이 문서를 읽은 뒤, "라이브 검증 체크리스트 (섹션 4)" 부터 시작한다.**
> 검증 통과하면 사용자에게 Phase 2 스펙 문의, 실패하면 FALLBACK 로그 원문 요청.
