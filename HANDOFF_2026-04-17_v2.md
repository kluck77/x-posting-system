# HANDOFF 2026-04-17

새 Claude 세션 또는 GPT가 이 repo를 맡게 되면 **먼저 이 문서를 읽고 CLAUDE.md도 같이 읽어라.**

---

## 현재 브랜치 / HEAD

- 브랜치: `claude/refactor-service-modules-9yIMA`
- HEAD: `b355e6a` (2026-04-17 기준)
- 배포 상태: 서버 반영됨 (`deploy-x` 필요 시 재실행)

---

## 서버 정보

| 항목 | 값 |
|---|---|
| IP | 107.191.61.190 |
| 서비스 | systemd `x-posting-bot` |
| DB | `/root/x-posting-system/x_poster.db` |
| venv | `/root/x-posting-system/venv/bin/python` |
| 대시보드 | http://107.191.61.190:8000/control/ |

## 배포 표준 (단 하나의 공식 명령)

```bash
deploy-x claude/refactor-service-modules-9yIMA
```

이 스크립트가 자동으로 하는 것:
1. xdashboard.service 부활 감지 → disable
2. systemd 밖 수동 python 프로세스 kill
3. `git pull origin <브랜치>`
4. `systemctl restart x-posting-bot`
5. 5초 후 서비스 상태 확인
6. 30초 동안 Conflict 에러 감시

**절대 하지 말 것:**
- 서버에서 직접 `python run.py` 수동 실행 금지
- `xdashboard.service` enable 금지
- `systemctl restart x-posting-bot` 단독 실행 금지

---

## 스케줄러 구조

| 시각 KST | 스케줄러 | 데이터 소스 |
|---|---|---|
| 매분 | `_news_monitor_loop` | RSS + Naver (_recent_items 200건 버퍼) |
| 04:00 | `_draft_cleanup_loop` | DB drafts |
| 05:00 | `_morning_digest_loop` | overnight_buffer (5AM 공식 경로) |

top5_scheduler는 제거됨. 공식 5AM 경로는 overnight_buffer로 단일화.

---

## 대시보드 구조 (5탭)

| 탭 | 역할 | 주요 기능 |
|---|---|---|
| Pulse | 상황판 | 시스템 상태 + 행동 카드 3개 + 큐 |
| Alerts | 실시간 피드 | Naver 게이지 + 긴급 큐 + 뉴스 (중요도 dot + 시간) |
| Agents | AI 팀 | 5개 에이전트 상태 + CSS 아바타 + provider LED |
| Pipeline | 작업판 | 2-view: 내 초안 / 실시간 후보 |
| Ops | 감사판 | Health + Eval + Business + Queue |

### Pipeline 2-view
- **내 초안**: 기존 draft 리스트 + [원문] [승인+전송] [거절 ✕] 버튼
- **실시간 후보**: 네이버/RSS 수집 기사를 키워드 점수로 정렬 (AI 비용 0)
  → "초안 생성" 버튼 누르면 그때만 AI 파이프라인 실행

---

## 점수 시스템 (3개)

### 1. Editorial Scores (content_pack.py)
- Alert (긴급도) / Postability (게시 적합도) / Trust (근거 신뢰도)
- 각 0~100, 자동 라우팅 기준

### 2. Resonance Score (content_pack.py - PR 35)
- 공명도 0~100, 8축:
  - Specificity 20 / Scannability 15 / Tension 15 / Reader Reward 15
  - Market Stake 10 / Beginner Clarity 10 / Social Currency 10 / Follow-Worthiness 5
- 라우팅: 85+ 게시 후보 / 70~84 수정 후 / <70 재생성
- Path A (Orchestrator) + Path B (generate_final_post) 둘 다 계산

### 3. 실시간 알림 점수 (news_monitor.py)
- _enhanced_score = _importance_score + 교차출처 + 최신성 - 쿨다운 패널티
- 임계값: 45+ → 텔레그램 후보 알림 (AI 비용 0)
- 일일 상한 8건 / 클러스터 1시간 쿨다운 / 22:00~05:00 차단

---

## AI 역할

| Role | Provider | 프롬프트 위치 |
|---|---|---|
| Researcher | Perplexity / Gemini | config.get_effective_research_provider() |
| DraftWriter | OpenAI (gpt-4o-mini) | app/providers/openai_provider.py L19-56 |
| FactChecker | Perplexity | - |
| Reviewer | Anthropic (Claude) | app/providers/anthropic_provider.py L27-92 |
| TrendHunter | Grok (grok-3-mini-fast) | app/providers/grok_provider.py |

**Grok mini-pass**: 대시보드 "전송" 버튼 클릭 시 `_handle_approve`에서 `quick_review()` 호출 → hook_alt / x_angle / resonance_note 반환 → verdict=use_grok_hook이면 hook 교체.

---

## API 엔드포인트

| Endpoint | 용도 |
|---|---|
| POST /ingest | 초안 생성 (Path A: Orchestrator) |
| POST /drafts/{id}/approve | 초안 승인 + Grok mini-pass |
| POST /drafts/{id}/reject | 초안 거절 |
| GET /control/status | 시스템 상태 |
| GET /control/editorial-summary | 점수 + 큐 |
| GET /control/providers | AI 역할 현황 |
| GET /control/flow-trace | 초안 리스트 (risk/score/url/broken/resonance) |
| GET /control/recent-news | 최근 기사 (title + time) |
| GET /control/scored-candidates | 점수 통과 기사 (AI 비용 0) |
| GET /control/naver | Naver API 사용량 |
| GET /control/business-summary | premium/b2b/newsletter |

---

## 파일 변경 요약 (이번 세션)

| 파일 | 변경 |
|---|---|
| app/main.py | morning_digest_loop 추가, top5_scheduler 제거 |
| app/db.py | generated_in_mock migration |
| app/models/content.py | Draft.generated_in_mock 컬럼 |
| app/orchestrator.py | _handle_approve + Grok mini-pass + ResonanceScore |
| app/services/news_monitor.py | _recent_items, _enhanced_score, _send_scored_alert |
| app/services/content_pack.py | compute_resonance_score, finalize 프롬프트 resonance |
| app/services/draft_service.py | is_broken_draft, broken_reason, generated_in_mock 저장 |
| app/services/api_cost_tracker.py | 전체 provider 항상 표시 |
| app/providers/openai_provider.py | DraftWriter 프롬프트 resonance |
| app/providers/anthropic_provider.py | Reviewer 프롬프트 resonance |
| app/providers/grok_provider.py | quick_review 메서드 |
| app/api/control_room.py | /scored-candidates, flow-trace 확장 |
| static/dashboard.html | 전면 리디자인 + 기능 |
| scripts/cleanup_broken_drafts.py | 깨진 초안 정리 |

---

## 남은 리스크 (우선순위)

### 즉시 실행
- **리스크 1**: Resonance 구조(⚠️/📌) 최종 초안에 미반영
  - 해결: `app/orchestrator.py` draft 저장 직전 후처리 자동 삽입

### 1일 이내
- **리스크 2**: 실시간 알림 임계값 45점 미검증 (24h 모니터링 후 조정)
- **리스크 3**: Grok mini-pass 품질/속도 미검증 (timeout 20→10 검토)

### 1주일 이내
- **리스크 4**: 자동 초안 생성 경로 없음 (55점+ 기사 5건/일 자동 실행)
- **리스크 5**: Voice Dupe 패턴 추가 (운영 데이터 기반)
- **리스크 6**: Grok 메타데이터 대시보드 표시

### 별도 PR
- **리스크 7**: Gemini 503 fallback 전략

---

## 작업 방식

1. **CLAUDE.md 먼저 읽기** — 배포 표준, 금지사항, 아키텍처 요점
2. **최소 수정 원칙** — broad refactor 금지, 모델 역할 재배치 금지
3. **테스트**: `pytest tests/` (1260개 통과 유지)
4. **커밋 메시지**: 한글 + `https://claude.ai/code/session_...` 포함
5. **배포**: `deploy-x <브랜치>` 단일 명령
6. **확인**: `journalctl -u x-posting-bot -f | grep -iE "에러|파이프라인"`

---

## 이전 핸드오프

- `HANDOFF_2026-04-11.md` — 이전 세션 스냅샷
- `HANDOFF_2026-04-17.md` — 2026-04-17 첫 핸드오프 (대시보드 리디자인 시점)
- **이 파일 (HANDOFF_2026-04-17_v2.md)** — 2026-04-17 최종 (AI 통합 + Grok)
