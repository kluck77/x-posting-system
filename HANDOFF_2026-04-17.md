# x-posting-system 세션 보고서
# 브랜치: claude/refactor-service-modules-9yIMA
# 날짜: 2026-04-17
# HEAD: dd676ed

---

## 이번 세션 완료 작업 요약

### 1. 5AM 브리핑 장애 진단 + Source-of-Truth 단일화

- **문제**: 5시 Top10 브리핑이 안 옴
- **원인**: `_top5_scheduler_loop`는 05:00에 실행되지만 `candidates=0` (news_monitor 자동 파이프라인 비활성)
- **해결**: 
  - `overnight_buffer`를 공식 5AM 경로로 승격
  - `_top5_scheduler_loop` 제거
  - `_morning_digest_loop` 05:01→05:00 정각 승격
  - `top5_briefing_service.py` 코드 유지 (테스트 의존)

### 2. Recent News 빈 화면 수정

- **문제**: overnight_buffer가 morning_digest 후 clear → 낮에 비어있음
- **해결**: `news_monitor.py`에 `_recent_items` 버퍼 추가 (최근 50건, clear 독립)
- `/control/recent-news`를 `_recent_items` 기반으로 전환

### 3. DB Health LED 버그 수정

- `control_room.py`: `db.execute("SELECT 1")` → `text("SELECT 1")` 래핑

### 4. Control Room 대시보드 대규모 리디자인

#### Design Tokens (v3 최종)
- bg 4단계: void `#07080c` / deep `#0c0d13` / surface `#181922` / raised `#232430`
- semantic colors: teal(healthy) / amber(warning) / red(critical) / indigo(active) / purple(business)
- text 3단계: pri `.93` / sec `.50` / mut `.20`
- typography: hero 32px / h1 24px / h2 20px / body 14px / label 10px / meta 9px

#### 탭별 구현
- **Pulse**: status-rail(LED 4개) + 32px hero headline + 시스템 진단 태그 + score gauge + KPI row + queue grid + footer
- **Alerts**: NAVER 스캔 애니메이션 + 긴급 큐 + 실시간 피드(중요도 색상 dot, 시간 표시) + footer
- **Agents**: CSS 컬러 아바타(D/R/S/F/T) + 4상태(작동/대기/차단/수동) + working sort + shimmer + summary + footer
- **Pipeline**: stage grid(flow line shimmer) + draft 2줄 구조(risk chip + monetization + 원문/전송/거절 버튼) + footer
- **Ops**: health rail(border-top green) + eval grid(has-data accent) + biz grid(purple) + queue panel(amber) + footer

#### 전체 한글화
- 모든 섹션 제목/상태 라벨/chip/summary 한국어
- 긴급/일일/새벽/주간, 대기/작동/수동/차단, 승인/거절/발행 등

#### 애니메이션
- LED breathe (box-shadow 3s), agent dot pulse (2s), agent avatar bounce (working만)
- shimmer bar (agent working + pipeline flow), naver scan line (3s)
- tab fadeIn (0.2s), list slideIn (stagger), score/queue transition

### 5. Pipeline 행동 기능 (신규)

- **점수 표시**: risk_level(안전/주의/위험) + monetization_score(💰N)
- **원문 버튼**: source_url → 새 탭 열기
- **승인+전송 버튼**: POST /drafts/{id}/approve → 텔레그램 전송
- **거절(✕) 버튼**: POST /drafts/{id}/reject → 목록에서 제거
- **버튼 상태**: 전송 중(dim) → ✓ 완료(green) → 실패(red, 재시도)
- **뉴스 시간 표시**: added_at → HH:MM 형식

---

## 커밋 이력

| 커밋 | 내용 |
|------|------|
| `9df2e15` | 모닝 다이제스트 05:01 KST 자동 스케줄러 등록 |
| `32767ce` | 대시보드 shell — fixed positioning + safe-area + dvh |
| `58e72fe` | Pulse/Alerts/Pipeline 카드 구조 + 정보 계층 개편 |
| `0979729` | Agents 4-state rows + Ops 계층 강화 + working motion |
| `21502ca` | control_room _get_health() DB 쿼리 text() 래핑 |
| `2a221cf` | 5AM source-of-truth를 overnight_buffer로 단일화 |
| `5925873` | Visual Language v2 — premium dark control room |
| `902cfd5` | Pulse/Alerts v3 — hero 28px, bg delta 12+, live feed |
| `1c02357` | Agents workstation + Pipeline throughput 재설계 |
| `49e19d6` | Ops audit panel + 전체 polish |
| `9843306` | idle 상태 가시성 강화 |
| `11f55da` | 카드 타입별 시각 차별화 + dead CSS 정리 |
| `a996e22` | 디자인 토큰 체계 정립 + shell polish |
| `d0bad8f` | Pulse control center + Alerts live feed 캐릭터 강화 |
| `4cf116a` | Ops audit panel — 섹션별 accent 분리 |
| `67feef4` | 전체 한글화 + 애니메이션 + 빈 공간 채움 |
| `91da740` | 에이전트 아바타 + 전체 애니메이션 + footer |
| `1bd9d03` | CSS 에이전트 아바타 + 시스템 진단 + 뉴스 중요도 |
| `206ddac` | Pipeline 초안에 점수 표시 + 승인/전송 버튼 |
| `1cc10ab` | 뉴스 기사에 수집 시간 표시 |
| `dd676ed` | Pipeline 초안 거절(✕) 버튼 추가 |

---

## 파일 변경 목록

| 파일 | 변경 |
|------|------|
| `app/main.py` | morning_digest_loop 추가, top5_scheduler_loop 제거, digest 05:00 |
| `app/services/news_monitor.py` | `_recent_items` 버퍼 + `get_recent_items()` |
| `app/api/control_room.py` | `text()` 수정, `/recent-news` → `_recent_items`, flow-trace joinedload + score/url |
| `static/dashboard.html` | 전체 리디자인 + 한글화 + 애니메이션 + Pipeline 버튼 |

---

## 미완료 / 다음 세션 작업

### 우선순위 1: news_monitor 자동 파이프라인 재활성화

**현재 상태**: news_monitor가 매분 기사 수집하지만 AI 분석/초안 생성을 안 함 (비용 방지로 주석 처리)

**필요한 것**:
1. 일일 호출 상한 (예: 하루 최대 20건만 AI 분석)
2. 중요도 상위 N건만 파이프라인 진입 (키워드 기반 필터)
3. URL 중복 체크 (같은 기사 중복 처리 방지)

**수정 파일**: `app/services/news_monitor.py` (주석 해제 + 가드 추가)

**이것 없으면**: Pipeline에 새 초안이 자동으로 안 들어옴. 수동 /ingest만 가능.

### 우선순위 2: Draft에 postability/trust score 직접 저장

**현재**: 점수가 eval_store의 eval_meta에만 있고 Draft 테이블에 없음
- Pipeline에서 risk_level + monetization_score로 대체 표시 중
- 정확한 P/T 점수 표시하려면 Draft 컬럼 추가 또는 eval_store 조인 필요

### 우선순위 3: DIGEST_TOP_N 설정

- `.env`에 `DIGEST_TOP_N=10` 추가하면 5→10건 다이제스트

---

## 서버 정보

- **IP**: 107.191.61.190
- **서비스**: systemd `x-posting-bot`
- **DB**: `/root/x-posting-system/x_poster.db`
- **venv**: `/root/x-posting-system/venv/bin/python`
- **대시보드**: http://107.191.61.190:8000/control/
- **배포**: `deploy-x claude/refactor-service-modules-9yIMA`
- **브랜치 HEAD**: `dd676ed`

---

## 현재 스케줄러 구조

| 시각 KST | 스케줄러 | 상태 |
|----------|---------|------|
| 매분 | `_news_monitor_loop` | ✅ 수집만 (AI 분석 없음) |
| 04:00 | `_draft_cleanup_loop` | ✅ |
| 05:00 | `_morning_digest_loop` | ✅ overnight_buffer 기반 |

## 현재 대시보드 API

| Endpoint | 용도 |
|----------|------|
| `/control/status` | health, queue, activity, news monitor |
| `/control/editorial-summary` | routing_queue, scores, eval counts |
| `/control/providers` | AI roles, mock_mode |
| `/control/flow-trace` | 초안 리스트 + risk + score + url |
| `/control/recent-news` | 최근 기사 제목 + 시간 |
| `/control/naver` | Naver API 사용량 |
| `/control/business-summary` | premium/b2b/newsletter |
| `POST /drafts/{id}/approve` | 초안 승인+전송 |
| `POST /drafts/{id}/reject` | 초안 거절 |
