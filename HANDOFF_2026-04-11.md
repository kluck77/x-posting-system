# X Posting System — 작업 인수인계서 (2026-04-11)

> **최신 갱신:** 세션 5 준비 단계 (UTC 2026-04-11 15:00 리셋 대기 중)  
> **읽는 순서:** §0 세션 4 요약 → §0.8 세션 5 진행 상태 → §4 남은 이슈 → §8 다음 스텝 → 나머지는 맥락용  
> **새 채팅방에서 이어가려면:** 이 문서 한 개만 읽으면 됨. 다른 핸드오프 문서 만들지 말 것.

---

## 0. 세션 4 — 최종 검증 결과 (읽기 필수)

**세션 목표 (3건):** ① KST 00:00 rate limit 리셋 확인 ② 한국어 manual 드래프트 1건 실제 텔레그램 카드 end-to-end 전송 ③ 서버 git HEAD → `claude/verify-snapshot-push-F6oaZ` 정렬.

**결과 한줄:** 코드 배포 & 수정 로직은 **검증 완료**. 그러나 ①②③ 모두 "물리적 관측"은 **다음 세션으로 이월** (블로커는 모두 기술 아닌 환경 제약).

### 0.1 확정된 사실 (3파일 배포 일치)

`diff origin/claude/verify-snapshot-push-F6oaZ` = **0 lines** (서버 워킹카피 기준):

| 파일 | 검증 지점 |
|---|---|
| `app/services/quality_scorer.py` | 한국어 패턴 6종 (`_INTERPRETATION_SIGNALS_KO`, `_MARKETABILITY_SIGNALS_KO`, `_REPEAT_SIGNALS_KO`, `_PILLAR_SIGNALS_KO`, `_WRONG_AUDIENCE_SIGNALS_KO`, `_RIGHT_AUDIENCE_SIGNALS_KO`) 정의 + 사용 grep 12 hit |
| `app/services/telegram_service.py` | 품질 게이트: `L79 quality_action` param, `L106 ❌ 거절 권장 (품질 미달)`, `L111 ❌ 거절 권장 (품질 경고 + 고위험)` |
| `app/services/rate_limiter.py` | **클래스 기반** (모듈 함수 아님). `KST = timezone(timedelta(hours=9))`, `_today_start()` KST-aware, `DEFAULT_MAX_TELEGRAM_PER_DAY = 5` |

서버 수정 기준 커밋: `1dcd581` (quality_scorer 한국어 패턴 추가). 브랜치 HEAD 에 반영됨.

### 0.2 Rate Limit 리셋 — 관측 블로커 (코드는 정상)

```
RateLimiter(session).get_today_telegram_count()  = 6
RateLimiter(session).can_send_telegram()         = (False, '일일 텔레그램 전송 한도 초과: 6/5')
DEFAULT_MAX_TELEGRAM_PER_DAY                     = 5
kst_today_start_utc                              = 2026-04-10 15:00:00 UTC
다음 리셋 예상 (KST 00:00)                        = 2026-04-11 15:00:00 UTC
```

서버 로그 실증:
```
[2026-04-11 00:58:12] WARNING rate_limiter 일일 텔레그램 전송 한도 초과: 6/5
[2026-04-11 01:39:05] WARNING rate_limiter 일일 텔레그램 전송 한도 초과: 6/5
(여러 건) [pipeline] approval card skipped (KO-only): draft_id=XXX
```

→ KST 기준 리셋 로직 자체는 정상. 단, 세션 시작 시점에 이미 6/5 초과 상태라 **신규 실전 전송이 물리적으로 막혀 있음**. 리셋 관측은 UTC 2026-04-11 15:00 이후 가능.

### 0.3 한국어 텔레그램 카드 실전 전송 — 회귀검증으로 대체 (A/B 결정적 증거)

신규 전송이 rate limit 으로 막혀 있어, **프로덕션 DB 에 이미 존재하는 드래프트를 NEW 코드로 다시 렌더링** 하는 레트로스펙티브 A/B 로 대체. 두 건 모두 `source_type=manual`.

**🎯 결정적 증거 — pk=819 (BOK 금리동결 기사, 한국어):**

| 항목 | OLD 코드 결과 | NEW 코드 결과 |
|---|---|---|
| 5대 기준 총점 | 20/100 | **33/100** (한국어 패턴이 잡힘) |
| action | reject | reject |
| scores | — | `{expertise:5, marketability:8, consistency:20, follower_quality:0, repeat_consumption:0}` |
| 🔬 품질 블록 | (표시됨) | `5대 기준 품질 분석: 33/100 + flags` |
| 📌 초안 우선순위 | 🔴 우선 | 🔴 우선 |
| 💡 추천 | ✅ 승인 가능 ← **버그** | **❌ 거절 권장 (품질 미달)** ← **수정 확인** |
| 3축 일관성 | ❌ 불일치 | ✅ 일치 |

→ 동일 드래프트에서 `NEW_CODE` 로 렌더링하면 **추천 라벨이 정확히 뒤집힌다**. 한국어 텍스트에 대해 품질 게이트가 올바르게 동작함을 의미.

**✅ 양성 대조군 — pk=809 (영어, high quality):**

```
[NEW total] 95/100  action=pass
카드 표시:
  (품질 블록 생략 — pass 이므로 정상 설계)
  📌 초안 우선순위: 🔴 우선
  💡 추천:       ✅ 승인 가능
→ 3축 일관 (pass → 승인가능, reject 아님)
```

→ NEW 코드가 고품질 샘플을 잘못 거절하지 않음 확인.

### 0.4 DB 백로그 & 전송 기록 (세션 4 시점 관측)

```
오늘 전송 완료 (6건 모두 서비스 재시작 이전):
  pk=819  KO  risk=low   msg=721  created=2026-04-11 00:28:17  (BOK)
  pk=809  KO  risk=low   msg=720  created=2026-04-10 23:59:56
  pk=807  EN  risk=med   msg=719  created=2026-04-10 23:56:16
  pk=804  EN  risk=med   msg=718  created=2026-04-10 23:40:41
  pk=796  EN  risk=med   msg=717  created=2026-04-10 23:08:32
  pk=789  EN  risk=med   msg=716  created=2026-04-10 22:42:49

pending 백로그: manual=6, naver_auto=57, news_api=1  (총 64)
최근 drafts pk: 839 (PENDING, 어떤 채널로도 나가지 않음)

xdashboard 재시작 시각: Sat 2026-04-11 02:21:30 UTC
세션 4 시작 이후 신규 Telegram 발송: 0건  ← rate limit 6/5 로 막힘
```

→ **중요:** 오늘자 6건의 전송은 모두 xdashboard 재시작 **이전** 이므로 OLD 코드가 전송한 것. 세션 4 에서 실전 NEW 코드 네트워크 전송은 일어나지 않았음. 그래서 §0.3 레트로스펙티브 A/B 로 동등 증거를 확보.

### 0.5 서버 git HEAD 정렬 — 블로커 (안전상 미수행)

```
현재 HEAD:    server-snapshot/20260410 (2ff0601)  ← detached
목표 HEAD:    claude/verify-snapshot-push-F6oaZ
체크아웃 시도: 실패 — "Your local changes would be overwritten by checkout"

워킹트리 상태:
  modified (18 files): quality_scorer.py, telegram_service.py, rate_limiter.py 등
  untracked (운영자 진행중 신규 서비스):
    app/services/breaking_alert_service.py
    app/services/breaking_classifier.py
    app/services/daytime_alert_service.py
    app/services/top5_briefing_service.py
    app/models/dedup.py
    + .bak / .orig
```

→ 운영자(사람)가 **검증 세션 밖에서** 진행 중인 별도 피처 브랜치 작업이 서버 워킹카피에 물려 있음. 세션 4 는 "검증만 수행, 수정 금지" 원칙이라 강제 checkout 금지. **운영자가 별도 세션에서 이 변경들을 본인 브랜치에 commit 한 뒤에 HEAD 정렬을 수행해야 함.**

### 0.6 다음 세션이 받아야 할 작업 (우선순위)

1. **[BLOCKED→물리 대기]** UTC 2026-04-11 15:00 이후 rate limit 리셋 확인:
   ```python
   # venv/bin/python
   from app.storage import get_session
   from app.services.rate_limiter import RateLimiter
   with get_session() as s:
       print(RateLimiter(s).get_today_telegram_count())
       print(RateLimiter(s).can_send_telegram())
   ```
   기대치: `0` / `(True, ...)`
2. **[사람 액션]** 리셋 확인 후, 한국어 manual 드래프트 1건을 대시보드에서 제출 → 실제 텔레그램 카드 스크린샷 1장 확보. 카드에 `🔬 5대 기준 품질 분석`, `📌 초안 우선순위`, `💡 추천` 3축이 일관되게 나와야 함.
3. **[사람 액션]** 운영자가 `breaking_alert_service`/`breaking_classifier`/`daytime_alert_service`/`top5_briefing_service`/`dedup` 관련 작업을 **별도 브랜치로 commit** 한 후:
   ```bash
   git stash push -m "session4-handoff" -- .   # 또는 본인 브랜치에 commit
   git fetch origin claude/verify-snapshot-push-F6oaZ
   git checkout claude/verify-snapshot-push-F6oaZ
   systemctl restart xdashboard
   ```
4. **[확인]** §0.1 파일 3종 diff=0 재확인 + `quality_scorer.py` 의 6종 `_*_SIGNALS_KO` 패턴 존재 확인.

### 0.7 세션 4 중 주의했던 함정 (다음 세션도 동일 주의)

- `RateLimiter` 는 **클래스**. 모듈 함수 호출하면 `ImportError: cannot import name 'get_today_telegram_count'`. 반드시 `RateLimiter(session).method()` 형태.
- 터미널이 `d.id` / `x.startswith` 같은 토큰을 자동으로 `<d.id>` / `<x.st>artswith` 로 브라켓 감쌈 (`.id` 가 인도네시아 TLD 로 인식됨). SQL 에서 `SELECT d.id FROM drafts d` 쓰지 말고 ORM `.filter()` 또는 `getattr(Draft, "id")` 사용.
- Korean 훅/본문 텍스트를 운영자 터미널에 붙여넣으면 쉘이 명령으로 해석해서 `hook:: command not found` 남발. 한국어 입력은 반드시 대시보드 폼에만 붙여넣을 것.
- 오늘자 6건 Telegram 성공 발송은 전부 xdashboard 재시작 **이전** (= OLD 코드). "Telegram 이 됐다" 는 로그만 보고 NEW 코드 검증 완료라 착각 금지. 네트워크 전송 검증은 반드시 재시작 **이후** 타임스탬프로.

### 0.8 세션 5 진행 상태 (리셋 대기 중)

**세션 5 목표:** 세션 4 에서 물리적 블로커로 이월된 3건 — ① telegram 카운터 실제 리셋 관측 ② 한국어 manual 1건 **실제** 네트워크 전송 ③ 텔레그램 카드 스크린샷 1장 확보. **서버 HEAD 정렬은 세션 5 범위 밖** (운영자 지시).

**세션 5 Part A 관측 결과 (UTC 2026-04-11 04:14:05):**
```
today_telegram_count = 6     ← 여전히 6 (아직 리셋 안 됨)
can_send_telegram    = (False, '일일 텔레그램 전송 한도 초과: 6/5')
_today_start         = 2026-04-10 15:00:00 (naive UTC) = KST 2026-04-11 00:00
```
→ rate_limiter 의 "오늘" 윈도우는 `[UTC 2026-04-10 15:00, UTC 2026-04-11 15:00)`. 오늘자 6건 전부 이 윈도우 안 (KST 07:42 ~ 09:28). **다음 실제 리셋: UTC 2026-04-11 15:00 = KST 2026-04-12 00:00.** 세션 5 Part A 실행 시점(UTC 04:14) 은 그보다 **10시간 46분 전**. 코드는 정상, 물리적 시간만 기다리면 됨.

**판정:** 옵션 1 (대기 후 재개). rate_limiter 상수/로직 우회 금지 (원칙 위반). DB 조작 금지 (세션 4 증거 파괴). 시계 조작 금지.

**재개 시 실행 순서 (Part A' → B → C):**
1. **Part A'** (reset 재확인, 1줄) — `today_telegram_count == 0` 확인
2. **Part B** (한국어 manual 실전 ingest) — `/ingest` POST, `source_type="manual"`, `language="ko"`, 비-금융 도메인 샘플 (§4 C skip 조건 회피)
3. **Part C** (log/DB 검증) — `server.log` 의 `텔레그램 카드 전송 성공: message_id=... draft_id=...` + DB row 조회로 `telegram_message_id` 확인
4. 운영자: 실제 텔레그램 카드 스크린샷 1장 확보 → 3축 (`🔬/📌/💡`) 육안 일관성 확인
5. 세션 5 최종 7섹션 리포트 작성

**재개 시 사전 준비 완료 항목 (대기 중 프리컴포즈):**
- Part A [7] 의 잘못된 import 수정: `from app.models.draft import Draft` → **`from app.models.content import Draft`** (실제 모델 위치 확인 완료, `app/models/content.py:94`)
- Part A' / B / C 원클립 스크립트: 세션 채팅방에 보관 (handoff 에는 중복 기록 안 함)
- 한국어 manual 샘플: "2026년 한국 최저임금 10,030원 확정" — 노동/정책 도메인 (KO-only skip 4개 도메인 `{금융, 투자, 크립토, 주식}` 바깥), 심층 분석 어조 (breaking 아님), 풍부한 "돈의 의미 해석" 앵글 (marketability/expertise/consistency/right-audience signal 전부 포함) → 승인 가능 루트 예상

**세션 5 미실행/금지 항목:**
- scorer / 추천 로직 / dashboard 수정 ❌
- 서버 git HEAD 정렬 ❌
- broad refactor ❌
- rate_limiter 상수 sed 스왑 ❌
- 이슈 F / G (아래) 수정 ❌ — 기록만

### 0.9 세션 5 Part A'/B/C 조기 실행 결과 (UTC 2026-04-11 04:35) — **참고만**

운영자가 VERDICT=STILL_BLOCKED 상태에서 Part A'/B/C 를 조기 실행함. 리셋 관측과 실제 전송은 당연히 미완료. **단, 부차 성과 있음:**

**draft 843 생성됨 (PENDING orphan):**
```
id                  = 843
source_item_id      = 858
category            = ContentCategory.ECONOMY
risk_level          = RiskLevel.MEDIUM
approval_status     = ApprovalStatus.PENDING
telegram_message_id = None                       ← 미전송 (rate limit 6/5 block)
version             = 1
created_at          = 2026-04-11 04:35:36
hook = "2026년 최저임금 10,030원 확정 - 301만 명 임금 상승이 만드는 2.4조원 소비력 이동"
```

**파이프라인 실행 기록 (server.log, draft 843):**
```
04:35:15  [2/6] Researcher    Gemini 404 (issue B, fail-open)
04:35:15  [3/6] DraftWriter   OperatorHints AttributeError (issue G, fail-open)
04:35:20  [4/6] FactChecker   Perplexity 401 (issue B, fail-open)
04:35:20  5-Criteria 결과     43/100 [reject] flags=Marketability/Consistency/Follower/Repeat WEAK
04:35:20  WARNING             REJECT → 재생성 시도 (최대 1회)
04:35:25  [5/6] Reviewer      최종 판단
04:35:36  [6/6] 분류 & 위험도 draft_id=843 category=economy risk=medium
04:35:36  WARNING rate_limiter 6/5 초과
04:35:36  WARNING orchestrator 텔레그램 전송 제한
```

**확정된 긍정 발견:**
1. **KO manual 입력이 KO-only skip 에 안 걸림.** `source_type=manual, language=ko, 비-금융 도메인 (노동/정책)` 조합은 breaking_classifier 를 통과해서 풀 AI 파이프라인으로 진입. §4 C 의 "language=ko → 무조건 skip" 이해가 부분적으로 틀렸음을 실증. 실제 skip 조건은 `breaking_classifier.classification ∈ {BREAKING_NOW, CANDIDATE} AND topic_domain ∈ {금융, 투자, 크립토, 주식}`.
2. **quality_scorer 한국어 패턴이 생성된 draft 에서 작동.** 세션 4 는 레트로스펙티브 A/B 였지만, 세션 5 는 세션 5 내에 생성된 draft 에 대해 Korean flags 출력 확인 (`⚠️ Marketability WEAK: 해외 독자 관련성 불명확` 등). 한국어 패턴이 세션 4 레트로에만 작동하는 게 아니라 runtime 에서도 작동함을 실증.
3. **파이프라인 전체 경로가 건강함.** Researcher/FactChecker fail-open, DraftWriter, 5-Criteria REJECT → 자동 재생성, Reviewer, 분류 단계까지 전부 정상 완료. 오직 마지막 telegram send 단계만 rate limit 으로 막힘.

**발견된 한계/이슈:**
- **43/100 reject 원인:** 운영자가 설계한 rich 한국어 source 를 DraftWriter 가 짧은 hook+body 로 압축하면서 marketability/consistency/follower/repeat 시그널이 유실됨. scorer 는 원본 source 가 아닌 생성된 draft 를 평가하므로, source 가 아무리 풍부해도 draft 가 압축되면 점수가 낮아짐.
- **"승인 가능" 루트 미달성.** 첫 시도는 reject 경로. 세션 5 재개 시 **DraftWriter 압축 후에도 살아남도록 설계된 강화 샘플**로 재시도 필요. 설계 원칙: 첫 문장에 결론+글로벌 앵글, 독자 세그먼트 명시, 단일 pillar 집중, 시리즈/추적 신호 명시.

**draft 843 처리 원칙:**
- 그대로 둠. DB 수정 / telegram_message_id 강제 세팅 / resend 우회 전부 금지.
- 리셋 후 재개 시 **새 샘플로 draft 844(+α) 생성 → 그쪽으로 스크린샷 확보**. draft 843 은 영구 orphan PENDING 으로 남김 (세션 4 의 레트로 A/B 증거와 병렬로 보존).

**draft 844 생성됨 (두 번째 조기 실행, UTC 2026-04-11 04:49, PENDING orphan):**
```
id                  = 844
source_item_id      = 859
category            = ContentCategory.ECONOMY
risk_level          = RiskLevel.MEDIUM
approval_status     = ApprovalStatus.PENDING
telegram_message_id = None                       ← 미전송 (rate limit 6/5 block 지속)
hook = "한국 산업용 전기요금 9.7% 인상, NVIDIA GPU 원가에도 영향"
```
- 5-criteria: **50/100 [warn]** (draft 843 의 43 reject → 50 warn 로 개선, WEAK flags 4→3)
- BusinessClassifier: score=75, b2b=True, tags=[growth, newsletter, b2b_candidate, data_story, sponsor_candidate]
- 압축-저항 설계 성공: Marketability/Consistency WEAK 해소. 잔존 WEAK: Expertise/Follower/Repeat.
- 처리 원칙: draft 843 과 동일. **그대로 두고, 리셋 후 이 draft 를 재전송 대상으로 사용**.

**Post-reset resend 경로 (세션 5 재개 시 사용):**
- **결론: HTTP 엔드포인트 없음. `Orchestrator.send_for_approval(draft_id)` 직접 호출 경로만 존재.**
- 로컬 `app/api/` 전수 조사 (admin.py + control_room.py): "기존 draft 를 Telegram 으로 재전송" 엔드포인트 부재.
  - `POST /ingest` — 새 draft 생성 + 자동 전송
  - `POST /drafts/{id}/approve` — X 게시 (텔레그램 아님)
  - `POST /drafts/{id}/reject` — 상태 변경만
  - `POST /drafts/{id}/retry` — `orchestrator.retry_failed` 호출, X 재게시 전용 (텔레그램 아님)
- 유일한 재전송 경로: `app/orchestrator.py:623` `send_for_approval(draft_id)` 직접 호출.
  - 동작: `rate_limiter.can_send_telegram()` → `draft_service.get_by_id(draft_id)` → `send_approval_card(draft)` → Telegram Bot API sendMessage → 성공 시 `draft_service.set_telegram_message_id(draft_id, message_id)`.
  - "이미 전송된 draft" 가드 없음. draft 844 의 `telegram_message_id=None` 상태에서 무조건 전송 시도.
  - `send_approval_card` 내부 `build_approval_card` 는 `score_5criteria` 를 재실행 (telegram_service.py:145-147) → 3축 카드 (🔬 5대 기준 + 📌 초안 우선순위 + 💡 추천) 완전 생성.

**Post-reset 실행 순서 (세션 5 재개 시, UTC ≥ 2026-04-11 15:00):**

1. **Part A' — 리셋 확인**
   ```bash
   python -c "from app.db import SessionLocal; from app.services.rate_limiter import RateLimiter; s=SessionLocal(); print('today_telegram_count=', RateLimiter(s).get_today_telegram_count()); print('can_send=', RateLimiter(s).can_send_telegram()); s.close()"
   ```
   기대: `today_telegram_count=0`, `can_send=(True, ...)` → `VERDICT=RESET_CONFIRMED`.

2. **draft 844 direct resend 시도 (최단 경로)**
   ```bash
   cd /root/x-posting-system && python -c "
   import asyncio
   from app.orchestrator import Orchestrator

   async def main():
       o = Orchestrator()
       try:
           ok = await o.send_for_approval(844)
           print(f'send_for_approval(844) returned: {ok}')
       finally:
           o.close()

   asyncio.run(main())
   "
   ```
   성공 조건: 반환값 `True`, 운영자 폰 Telegram 알림 수신, 카드에 `초안 ID: 844` 표시, DB `drafts.id=844.telegram_message_id` 가 `None` → 정수로 갱신.

3. **(실패/불가 시 fallback) 전기요금 샘플 재-ingest**
   ```bash
   curl -sS -X POST http://localhost:8000/ingest \
     --data-binary @/tmp/s5_payload.json \
     -H 'Content-Type: application/json'
   ```
   → 새 draft 845 (또는 그 이상) 생성. 동일 전기요금 소스. 단점: draft 844 추적 연속성 소실, Part C' 재작성 필요.

4. **Telegram 카드 수신 + 스크린샷 확보**
   - 운영자 폰에서 실제 카드 캡처 1장.
   - 검증 포인트: (a) 3축 전부 표시 (5대 기준 / 초안 우선순위 / 추천), (b) 초안 ID, (c) warn-route 답지 (`⚠️ 검토 권장` 또는 quality_action='warn' + risk=medium 경로).

5. **Fixed Part C-3 / C-4 — DB-로그 검증** (터미널 auto-bracket 회피 `getattr` 패턴 사용)
   ```python
   from sqlalchemy import and_
   from app.db import SessionLocal
   from app.models.content import Draft, SourceItem

   D_pk      = getattr(Draft, "id")
   D_src     = getattr(Draft, "source_item_id")
   D_tg      = getattr(Draft, "telegram_message_id")
   D_created = getattr(Draft, "created_at")
   SI_pk     = getattr(SourceItem, "id")

   # kst_midnight_utc 는 RateLimiter._today_start() 재사용 권장
   from app.services.rate_limiter import RateLimiter
   s = SessionLocal()
   kst_midnight_utc = RateLimiter(s)._today_start()

   rows = (s.query(Draft, SourceItem)
             .join(SourceItem, D_src == SI_pk)
             .filter(and_(D_tg.isnot(None), D_created >= kst_midnight_utc))
             .order_by(D_pk.desc()).all())
   for d, si in rows:
       print(d.id, d.telegram_message_id, si.source_type, d.hook[:50])
   s.close()
   ```
   기대: 최소 1행 — draft 844 (또는 fallback 시 845) 가 `telegram_message_id` 세팅되어 등장.

6. **최종 7항목 보고** (운영자 지정 섹션)
   - Confirmed Facts / Reset Check Result / Actual Test Input / Telegram Card Result / DB-Log Result / Evidence / Recommendation

**Post-reset 실행 원칙:**
- 실행은 반드시 **UTC 2026-04-11 15:00 이후** 에만. 그 전 재실행 금지.
- 목표는 **warn-route 카드 1장 + 3축 정합성 확인**. "승인 가능" 루트 집착 금지 (DraftWriter 프롬프트 수정 필요 → 세션 범위 밖).
- draft 843/844 DB 수정 금지. 우회 금지. rate_limiter sed swap 금지.
- scorer/recommendation/dashboard/HEAD 정렬 금지.

---

### 0.10 세션 6 — Top5 05:00 KST 브리핑 수정 (Fix A + Fix B')

**배경:** 세션 5까지 05:00 KST Top5 브리핑이 매일 미전송. 운영자 로그로 스케줄러는 돌았으나 카드가 나가지 않음을 확인.

**원인 진단 (확정):**
`app/services/top5_briefing_service.py::_current_cycle_date()` 의 경계 오프셋 버그.
- 기존 로직: `if now_kst.hour < 5: return today else: return today+1`
- 22:30 KST 수집분은 `cycle_date = today+1` 로 저장됨 (의도대로).
- 05:00:00.xxx 스케줄러 진입 시 `hour < 5` 가 False → `cycle_date` 계산이 또 `today+1` 로 플립되어, 실제로는 "다음 날+1" 을 조회. 하지만 더 근본적으로, 22:00~04:59 에 수집된 후보들이 "오늘 05:00 브리핑" 용으로 당시 `today+1 = today(브리핑 일)` 로 저장되는데, 05:00:00 실행 시 `today+1` 로 다시 플립되면서 익일 cycle 을 보게 되어 조회 결과가 **상시 0건**이었다.
- 동일 헬퍼를 쓰는 4개 경로 — write(candidate), write(breaking_sent), read(load_candidates), cleanup — 이 일관되게 오염.

**적용 Fix:** (파일 1개, 함수 4개, 라인 순서 보존)

- **Fix A (`_current_cycle_date`, line 128)**: 05:00:00~05:00:59 KST 를 "당일" 사이클에 포함하도록 경계 조건을 `hour < 5 or (hour == 5 and minute == 0)` 으로 확장. 05:01 부터만 "익일" 사이클로 플립.
- **Fix B'-1 (`select_top5`, line 437)**: `fallback: bool = False` 키워드 파라미터 추가. 60점 하한선(`_SCORE_THRESHOLD=60`) 적용 분기를 `if not fallback:` 로 감쌈. 나머지 로직(야간 필터, BREAKING 제외, 중복 제거, 자산군 캡, Top5 제한)은 변경 없음.
- **Fix B'-2 (`build_top5_briefing_text`, line 537)**: `is_fallback: bool = False` 키워드 파라미터 추가. fallback 모드에서는 헤더를 `"상위 후보 {N}건"`, 기준 라인을 `"야간 수집 후보 상위 요약"` 으로, 그리고 `"오늘은 60점 이상 후보가 없어, 야간 상위 후보를 대신 정리했습니다."` 참고 라인을 삽입. 카드 본문 구조와 URL 섹션은 동일.
- **Fix B'-3 (`send_top5_briefing`, line 603)**: 2-pass 선정. 1차 정상 선정 → 0건이면 2차 `fallback=True` 선정 → 2차도 0건이면 `"야간 후보 자체가 0건 — 05:00 브리핑 생략"` 로그 후 True 반환. 1차 성공 시엔 정상 카드, 2차 성공 시엔 fallback 카드. 성공 로그에 `mode=normal|fallback` 태그 추가.

**전송 정책 (확정):**
| 60점 이상 | 야간 수집 | 결과 |
|---|---|---|
| 5건 이상 | — | 정상 Top5 카드 |
| 1~4건 | — | 정상 Top N 카드 |
| 0건 | 1건 이상 | **fallback 카드** (60점 하한선 해제, 상위 N건) |
| 0건 | 0건 | 브리핑 생략 (로그만) |

**검증 결과:**
- `python -m py_compile app/services/top5_briefing_service.py` → OK
- 토큰 오염 grep `<logger\.|<now_|<ref_|<\.\.\.>` → 0건
- 인라인 스모크 테스트 7/7 통과:
  - boundary: 04:59:59=today / **05:00:00=today (bug 수정 증거)** / 05:00:30=today / 05:01:00=tomorrow / 22:30=tomorrow / 00:30=today
  - normal path: 고점수 후보 1건 → `select_top5()` 반환 1건 (score=92)
  - fallback path: 저점수 후보만 → normal 0건 / fallback 1건
  - fallback 카드 텍스트: `"상위 후보"` + `"60점 이상 후보가 없어"` 문구 포함 확인
  - normal 카드 텍스트: `"Top 1"` 포함, fallback 문구 없음 확인
  - 완전 빈 상태: normal 0건 / fallback 0건 (생략 경로)
- 기존 pytest 스위트: 변경 전과 동일한 pre-existing failures 만 발생 (test_config / test_providers mocks). Top5 관련 테스트는 원래 없음. 새 테스트 파일은 생성하지 않음 (broad refactor 금지).

**변경되지 않은 것 (명시):**
- `orchestrator.py` / `main.py` / `telegram_service.py` / `rate_limiter.py` — 전부 무수정
- DB 스키마 무변경 (candidate_pool_entries / breaking_sent_keys 그대로)
- Session 5 post-reset 라인 (draft 844 resend) — 독립 트랙으로 유지

**서버 적용 절차 (운영자 실행용 — 이 코드는 sandbox 에만 존재):**

1. **Pre-change DB diagnosis (필수: 적용 전에 반드시 1회 기록)**
   ```bash
   cd /root/x-posting-system
   sqlite3 x_poster.db "
   SELECT 'candidate_pool_entries', cycle_date, COUNT(*)
     FROM candidate_pool_entries GROUP BY cycle_date ORDER BY cycle_date;
   SELECT 'breaking_sent_keys', cycle_date, COUNT(*)
     FROM breaking_sent_keys GROUP BY cycle_date ORDER BY cycle_date;
   SELECT 'total_candidates', COUNT(*) FROM candidate_pool_entries;
   "
   ```
   → 출력 저장. 예상: 과거 cycle_date 에 orphan row 다수 누적 (미삭제 상태).

2. **Backup**
   ```bash
   cp /root/x-posting-system/x_poster.db /root/x-posting-system/x_poster.db.bak_session6
   ls -l /root/x-posting-system/x_poster.db.bak_session6
   ```

3. **Git pull & 정적 검증**
   ```bash
   cd /root/x-posting-system
   git fetch origin claude/verify-snapshot-push-F6oaZ
   git checkout claude/verify-snapshot-push-F6oaZ
   git pull origin claude/verify-snapshot-push-F6oaZ
   python -m py_compile app/services/top5_briefing_service.py && echo OK
   ```

4. **(선택) Orphan cleanup — 과거 cycle_date 삭제**
   ⚠️ 적용 후 다음 05:00 브리핑이 정상 작동하는 것을 먼저 확인한 뒤에만 실행 권장.
   ```bash
   sqlite3 /root/x-posting-system/x_poster.db "
   DELETE FROM candidate_pool_entries WHERE cycle_date < date('now', '+9 hours');
   DELETE FROM breaking_sent_keys WHERE cycle_date < date('now', '+9 hours');
   "
   ```

5. **서버 재시작**
   ```bash
   systemctl restart xdashboard
   sleep 3
   systemctl status xdashboard --no-pager | head -20
   journalctl -u xdashboard -n 30 --no-pager | grep -i 'top5\|briefing'
   ```

6. **다음 05:00 KST 관측 지점**
   - 05:00:00~05:00:05 KST 로그: `[top5] 05:00 브리핑 시작 ref=...`
   - 이어서 둘 중 하나:
     - `[top5] 브리핑 전송 성공 ({N}건, mode=normal)` — 60점 이상 후보가 있었음
     - `[top5] 60점 이상 후보 0건 — fallback 선정 시도` → `[top5] 브리핑 전송 성공 ({N}건, mode=fallback)` — fallback 경로
     - 또는 `[top5] 야간 후보 자체가 0건 — 05:00 브리핑 생략` — 야간 수집이 정말 0건
   - `[top5] DB 로드 완료: candidates=..., sent_keys=... (cycle=YYYY-MM-DD)` 에서 cycle 값이 **브리핑 실행일** 과 일치해야 함 (이전 버그에선 익일이 찍혔음).
   - 텔레그램 카드 수신 여부 육안 확인.

### 0.11 세션 6 수동 검증 결과 (2026-04-12 09:24 KST)

**서버 반영:**
- `git show origin/claude/verify-snapshot-push-F6oaZ:app/services/top5_briefing_service.py` 로 1파일만 덮어쓰기 (서버 워킹트리 dirty → checkout 불가 → 우회)
- 643 → 691 lines 확인, py_compile OK
- xdashboard 재시작 완료 (00:08:32 UTC)
- 서버 git HEAD 는 여전히 `server-snapshot/20260410` (top5 파일만 세션6 버전)

**서버 반영 시 주의:**
- 서버에 `sqlite3` CLI 미설치 → DB 조회는 반드시 venv python 사용
- 서버 워킹트리 dirty (18 modified + 7 untracked) → `git checkout` 불가 → `git show origin/branch:path > path` 방식으로 개별 파일 반영

**DB 진단 (반영 직전):**
- candidate_pool_entries: 2026-04-10 (2건), 2026-04-11 (749건), 2026-04-12 (95건), 2026-04-13 (37건)
- breaking_sent_keys: 0건
- DB 백업: `x_poster.db.bak_session6` (1.3M)

**수동 시뮬레이션 (ref_kst=2026-04-12T05:00:00 KST 고정):**
- cycle=2026-04-12 후보 95건 로드
- 야간 윈도우(04-11 22:00~04-12 05:00) 통과: 12건
- normal 선정: 0건 (60점 이상 없음)
- fallback 선정: 5건 (scores: 53, 48, 47, 47, 44)
- HTTP 200 OK → `[top5] 브리핑 전송 성공 (5건, mode=fallback)` → `send_result=True`
- **텔레그램 카드 수신 확인**: `[새벽 5시 브리핑] 2026-04-12 — 상위 후보 5건`
- fallback 헤더 / 기준 라인 / 참고 라인 전부 정상
- DB cleanup 미실행 (send_top5_briefing 만 호출, run_top5_briefing 아님) → 내일 05:00 자연 발동에 영향 없음

**판정: Fix A + Fix B' 전체 파이프라인 실서버 실증 완료.**

---

## 1. 프로젝트 개요
- **저장소:** `kluck77/x-posting-system`
- **운영 서버:** Vultr, `/root/x-posting-system`, systemd 서비스 `xdashboard`
- **작업 브랜치:** `claude/verify-snapshot-push-F6oaZ` (GitHub ↔ 서버 파일 동기화 완료, HEAD 포인터 정렬은 §0.5 블로커로 대기)
- **스택:** FastAPI + SQLite + Claude(Anthropic) + OpenAI + Telegram Bot API
- **역할 분담:**
  - **ChatGPT (gpt-4o-mini)** → DraftWriter (초안 작성)
  - **Claude (claude-sonnet-4-20250514)** → Reviewer (리스크 판단·팩트체크·최종 다듬기, 시스템의 "두뇌")
  - **Telegram** → 사람 승인 게이트 (승인/거절/보류/재생성)

## 2. 시스템 철학 (변하면 안 되는 것)
- 뉴스 요약 계정 아님. **"돈의 의미 해석"** 계정.
- 모든 포스트는 시장/자본 관점 1줄 이상 포함 필수.
- 사람 승인 없이 자동 게시 안 함 (Telegram 게이트).
- 한국 금융/경제/정책 이슈 → 국제 독자에게 해석.

## 3. 최근 완료 작업: "출력 언어 한국어 기본 통일"

### 배경
이전에는 영어 기본이었고, 승인 카드가 라벨·내용·값 모두 영어/혼용이었음.
운영자(한국어 사용자)가 승인 판단을 빠르게 하기 어려움 → **한국어 기본화** 결정.

### 원칙
- 운영 기본 언어 = 한국어.
- 영어는 `language="en"` 명시 시에만 허용 (그래도 Reviewer 결과물은 한국어로 다듬어짐).
- 새벽 Top5, Breaking, KO-only 라인 전부 한국어.
- DB 스키마·orchestrator 전면 교체 **금지**, 최소 수정 원칙.
- 실제 텔레그램 카드 렌더링으로 검증 필수.

### 커밋 체인 (5개, 전부 origin 반영됨)

| 커밋 | 내용 | 주요 파일 |
|---|---|---|
| `e8cf3d7` | 출력 언어 한국어 기본 통일 | `app/config.py` (default_language="ko"), `app/models/content_request.py`, `app/orchestrator.py` (재생성 2곳) |
| `eff5632` | Reviewer/DraftWriter 한국어 전환 + Telegram 카드 라벨 한국어화 | `app/providers/anthropic_provider.py` (REVIEW_SYSTEM_PROMPT 한국어), `app/providers/openai_provider.py`, `app/services/telegram_service.py` (라벨 전부 한국어) |
| `b6790e9` | REVIEW_SYSTEM_PROMPT 트리플 따옴표 문법 오류 수정 | `app/providers/anthropic_provider.py` (`}""""""` → `}"""`) |
| `e05d51f` | DraftWriter 파라미터 추가 + Reviewer JSON 추출 fallback | 두 provider에 `source_type`, `criteria_context` 파라미터 추가. Claude 한국어 출력이 JSON 앞뒤에 텍스트 붙이는 경우 `re.search(r'\{.*\}', ..., re.DOTALL)` 로 추출 |
| `11298e5` | approval 카드 **내부 값**도 한국어 전환 | `app/services/telegram_service.py` — 매핑 딕셔너리 5종 추가 |

### telegram_service.py — 추가된 매핑 딕셔너리
```python
_CATEGORY_KO = {
    "politics": "정치", "policy": "정책", "economy": "경제",
    "society": "사회", "kpop_culture": "K-POP/문화", "evergreen": "에버그린",
}
_RISK_KO = {"low": "낮음", "medium": "보통", "high": "높음"}
_CTA_KO = {
    "newsletter_signup": "뉴스레터 구독", "premium_waitlist": "프리미엄 대기",
    "thread_follow": "스레드 팔로우", "poll_engage": "투표 참여",
}
_ASSET_KO = {
    "newsletter_push": "뉴스레터", "premium_teaser": "프리미엄 티저",
    "thread_series": "스레드 시리즈", "community_post": "커뮤니티 포스트",
}
_BIZ_TAG_KO = {
    "growth": "성장", "newsletter": "뉴스레터", "premium_candidate": "프리미엄 후보",
    "data_story": "데이터 스토리", "sponsor_candidate": "스폰서 후보",
}
```

### 최종 검증 (draft_id=819, 2026-04-11)
- Input: `language="en"`, BOK 기준금리 동결 영어 소스
- 응답: `telegram_sent=true`, `category=economy`, `risk_level=low`
- **Reviewer가 영어 소스를 받아 한국어 hook/body로 다듬어 반환**
- 텔레그램 카드 전체 한국어 렌더링 확인:
  - `📨 새 초안 검토 요청`, `🎯 훅`, `📝 본문`
  - `카테고리: 경제`, `위험도: 낮음`
  - `5대 기준 품질 분석`
  - `비즈니스: 성장, 프리미엄 후보, 뉴스레터, 데이터 스토리`
  - `CTA: 프리미엄 대기`, `자산: 프리미엄 티저`
  - `추천: ✅ 승인 가능`
  - 버튼: `승인 / 거절 / 보류 / 재생성`

## 4. 알려진 잔존 이슈 (다음 스텝 후보)

### ✅ A. rate_limiter UTC midnight 버그 — **수정 완료** (`claude/verify-snapshot-push-F6oaZ` 반영)
- 위치: `app/services/rate_limiter.py`
- 현재 상태: **클래스 기반 RateLimiter** 로 재작성됨. `KST = timezone(timedelta(hours=9))`, `_today_start()` 가 KST-aware. `DEFAULT_MAX_TELEGRAM_PER_DAY = 5`.
- 세션 4 검증 (§0.2): 함수 호출 기준으로 `today_telegram_count=6`, `can_send_telegram=(False, '…6/5')` 정상 동작. 리셋 **로직**은 정상, 리셋 **관측**은 UTC 2026-04-11 15:00 이후.
- 호출 형태 주의: `RateLimiter(session).get_today_telegram_count()` — 모듈 함수 호출 금지 (ImportError).
- 구 버전 회피 꼼수 (`DEFAULT_MAX_AI_DRAFTS_PER_DAY = 20 ↔ 800` sed 스왑) 는 **더 이상 필요 없음**. §5 의 "⚠️ rate_limiter 임시 우회 패턴" 섹션은 이력용으로만 유지.

### 🟡 B. Provider 장애
- **Gemini** → 404 (모델명/엔드포인트 변경 가능성, 확인 필요)
- **Perplexity** → 401 (API 키 만료 또는 교체 필요)
- 현재는 OpenAI + Anthropic 만으로 운영 중 (문제없음)

### 🟡 C. KO-only 파이프라인과 승인 카드
- `language="ko"` 입력 → KO-only 라인으로 빠져서 DraftWriter/Reviewer 스킵, 텔레그램 승인 카드 **안 감**.
- 응답: `"KO-only pipeline (English draft skipped). domain=금융"`
- 이게 의도된 설계인지 확인 필요. "한국어 기본" 목표 대비, **한국어 입력도 승인 카드를 타야 하지 않나?** 는 논의거리.
- 현재는 EN 입력만 approval card 흐름을 탐 (Reviewer가 한국어로 다듬어서 카드 생성).

### 🟢 D. DraftWriter fallback 로직
- OpenAI DraftWriter가 실패하면 하드코딩된 `🇰🇷 {title}` 로 폴백되었던 버그를 `e05d51f`에서 파라미터 추가로 해결.
- 추가 회귀 방지 위해 Reviewer JSON 파싱도 regex fallback 추가됨.

### 🟡 G. `OperatorHints` 주입 AttributeError (세션 5 Part B 관측, **수정 금지 — 기록만**)
- 위치: `app/orchestrator.py` (DraftWriter 단계 직전, OperatorHints 주입 로직)
- 증상 (server.log, UTC 2026-04-11 04:35:15):
  ```
  WARNING [OperatorHints] 주입 실패 (무시):
    'DraftService' object has no attribute 'get_recent_operator_hints'
  ```
- 동작: fail-open. 주입 실패 → 무시하고 파이프라인 계속. draft 843 은 정상적으로 pipeline 끝까지 진행됨.
- 원인 추정: `DraftService` 클래스에 `get_recent_operator_hints()` 메서드가 정의되지 않았거나, 리팩토링 과정에서 이름이 바뀌었을 가능성. orchestrator 쪽은 옛 이름으로 호출.
- 영향: 오퍼레이터 힌트 피드백 루프가 현재 작동하지 않음. 품질 스코어/리뷰어 판단에 영향 가능성.
- 세션 5/6 원칙: **수정 금지.** 다음 별도 세션에서:
  1. `app/services/draft_service.py` 또는 유사 파일에서 현재 존재하는 operator hints 관련 메서드 확인
  2. orchestrator 호출부 메서드 이름 정합
  3. 또는 OperatorHints 기능 자체가 deprecated 이면 호출 코드 제거

### 🟡 F. `daily_summary.drafts.used` 집계 이상치 (세션 5 Part A 관측, **수정 금지 — 기록만**)
- 위치: `app/services/rate_limiter.py` — `get_daily_summary()` 의 `drafts` 키
- 관측 (UTC 2026-04-11 04:14:05):
  ```
  daily_summary = {
    'drafts':      {'used': 72, 'limit': 5},      ← used 가 limit 의 14배
    'ai_pipeline': {'used': 8,  'limit': 20, ...}, ← 정상
    'telegram':    {'used': 6,  'limit': 5},       ← 정상 (한도 초과 상태)
    'posts':       {'used': 0,  'limit': 3},       ← 정상
  }
  ```
- 의심: `drafts.used` 카운터가 사용하는 집계 윈도우/필터가 `telegram` 이나 `ai_pipeline` 과 다를 가능성. 혹은 `drafts.limit` 상수가 다른 키와 다른 의미(예: telegram 전송 한도를 그대로 복사). `used=72` 자체는 "오늘 생성된 drafts row 총 개수"(KO-only skip 포함) 일 가능성이 큼.
- 영향: telegram 리셋 검증과 **직접 관련 없음**. 세션 5 는 `telegram.used/limit` 만 추적하면 됨.
- 세션 5/6 원칙: **수정 금지.** 다음 별도 세션에서:
  1. `rate_limiter.get_daily_summary` 의 `drafts` 블록 정의 확인
  2. `DEFAULT_MAX_AI_DRAFTS_PER_DAY` 상수와 `drafts.limit` 의 관계 확인
  3. `used` 가 실제 어떤 쿼리로 집계되는지 확인 (KO-only row 포함 여부, source_type 필터 여부)
  4. 의도된 동작이면 필드 이름/주석 정정, 버그면 집계 로직 수정

## 5. 서버 운영 가이드 (중요한 gotcha들)

### ⚠️ 터미널 URL 자동 브라켓
사용자 터미널이 `http://foo` 형태를 감지하면 자동으로 `<http://foo>`로 감싸서 curl이 깨짐. **heredoc 안에서도 발생.**

**해결 패턴:**
```bash
S=http
C=:
SL=/
SEP="${C}${SL}${SL}"
HOST=localhost
PORT=8000
API="${S}${SEP}${HOST}${C}${PORT}/ingest"
curl -sS -X POST "$API" ...
```
→ 스크립트 내에 `http://` 문자열이 한 번도 등장하지 않아야 함.

### 🗄️ (구) rate_limiter 임시 우회 패턴 — **더 이상 사용 금지**
세션 4 시점에 `rate_limiter.py` 는 이미 KST-aware 클래스로 재작성됨 (§4 A 참조). 아래 sed 스왑 패턴은 **이력 보존용**이며, 새 세션은 절대 적용하지 말 것:
```bash
# (사용 금지 — 이력용)
# sed -i 's/DEFAULT_MAX_AI_DRAFTS_PER_DAY = 20/DEFAULT_MAX_AI_DRAFTS_PER_DAY = 800/' app/services/rate_limiter.py
```
현재는 `DEFAULT_MAX_TELEGRAM_PER_DAY = 5` (일일 5건) 로 운영. 초과 시 KST 00:00 에 자동 리셋.

### ⚠️ git checkout 시 `origin/` 접두사
서버에서 브랜치 최신 파일 반영할 때:
```bash
git fetch origin claude/verify-snapshot-push-F6oaZ
git checkout origin/claude/verify-snapshot-push-F6oaZ -- <파일경로>
```
`origin/` 없이 `claude/verify-snapshot-push-F6oaZ`만 쓰면 **로컬 브랜치 기준**으로 구버전 파일이 나옴 (경험상 크게 당함).

### ⚠️ 명령은 합쳐서 한 번에
사용자 피드백: 명령을 쪼개서 보내면 느리고 짜증남. **heredoc 스크립트 하나로 묶어서** 한 번에 실행하는 걸 선호.

## 6. 스모크 테스트 템플릿

### 사전 조건 (세션 4 후)
- rate_limiter sed 스왑은 **더 이상 사용 금지** (§4 A).
- `RateLimiter(session).can_send_telegram()` 가 `(True, ...)` 일 때만 테스트 의미 있음. 막혀 있으면 KST 00:00 리셋 대기.

### EN 입력 → 한국어 승인 카드 생성 (카드 검증 목적)
```bash
cat > /tmp/verify_en.sh << 'SCRIPT'
set -e
cd /root/x-posting-system

# (세션 4 전) sed 스왑은 제거됨 — 현재는 KST-aware rate limit 을 그대로 사용

S=http; C=:; SL=/; SEP="${C}${SL}${SL}"; HOST=localhost; PORT=8000
API="${S}${SEP}${HOST}${C}${PORT}/ingest"

curl -sS -X POST "$API" -H "Content-Type: application/json" \
  -d '{"title":"BOK holds benchmark rate","source_text":"Bank of Korea held its benchmark rate at 2.75 percent for the sixth month.","source_type":"manual","language":"en"}' \
  -w "\nHTTP=%{http_code}\n"
SCRIPT
bash /tmp/verify_en.sh
```

### 한국어 manual 드래프트 → 실전 카드 (세션 5 핵심 테스트)
```bash
cat > /tmp/verify_ko.sh << 'SCRIPT'
set -e
cd /root/x-posting-system

S=http; C=:; SL=/; SEP="${C}${SL}${SL}"; HOST=localhost; PORT=8000
API="${S}${SEP}${HOST}${C}${PORT}/ingest"

curl -sS -X POST "$API" -H "Content-Type: application/json" \
  -d '{"title":"한국은행 기준금리 동결","source_text":"한국은행이 기준금리를 연 2.75%로 여섯 달 연속 동결했다.","source_type":"manual","language":"ko"}' \
  -w "\nHTTP=%{http_code}\n"
SCRIPT
bash /tmp/verify_ko.sh
```
기대치: `telegram_sent=true` + 실제 카드에 `🔬 품질 / 📌 우선순위 / 💡 추천` 3축 일관 표시. `approval card skipped (KO-only)` 로 빠지면 §4 C 논의 필요.

## 7. 파일 맵 (이번 작업으로 수정된 핵심 파일)

```
app/
├── config.py                          # default_language="ko"
├── models/
│   └── content_request.py             # language="ko" 기본
├── orchestrator.py                    # 재생성 2곳 language 유지 로직
├── providers/
│   ├── anthropic_provider.py          # REVIEW_SYSTEM_PROMPT 한국어 + JSON fallback
│   └── openai_provider.py             # DraftWriter 한국어 기본 + 파라미터 추가
└── services/
    ├── telegram_service.py            # 라벨·값 모두 한국어 + 매핑 딕셔너리 5종 + quality_action 게이트(L79/L106/L111)
    ├── rate_limiter.py                # ✅ 클래스 기반 KST-aware (세션 4 검증 완료, §4 A)
    └── quality_scorer.py              # ✅ 한국어 패턴 6종 추가 (1dcd581, §0.1)
```

## 8. 다음 스텝 제안 (우선순위) — 세션 6 후 갱신

1. **[🔴 High / 사람액션]** §0.10 서버 적용 절차 1~5 실행 후 **다음 05:00 KST 브리핑 관측**. 로그 `[top5] 브리핑 전송 성공` 또는 `[top5] fallback 선정 시도` 확인, 텔레그램 카드 수신 스크린샷 1장 확보.
2. **[🔴 High / 물리대기]** UTC 2026-04-11 15:00 이후 rate limit 리셋 실측 확인. 명령 스니펫은 §0.6-1.
3. **[🔴 High / 사람액션]** 리셋 확인 후 한국어 manual 드래프트 1건 실제 텔레그램 전송 + 카드 스크린샷 1장 확보. 3축(`🔬/📌/💡`) 일관성 육안 확인.
4. **[🔴 High / 사람액션]** 운영자가 서버 워킹트리에 있는 신규 서비스(`breaking_alert_service`, `breaking_classifier`, `daytime_alert_service`, `top5_briefing_service`, `dedup`) 를 **본인 브랜치에 commit** 후, 서버 git HEAD 를 `claude/verify-snapshot-push-F6oaZ` 로 정렬. 상세는 §0.5-§0.6-3.
5. **[🟡 Mid]** KO-only 파이프라인도 승인 카드 흐름에 태울지 아키텍처 결정. 현재는 `[pipeline] approval card skipped (KO-only)` 로 빠지고 있음 (로그에서 여러 건 관측). 태우기로 하면 orchestrator 수정 필요.
6. **[🟡 Mid]** Gemini 404 / Perplexity 401 원인 확인 후 API 키/모델명 교체 또는 프로바이더 비활성화.
7. **[🟡 Mid]** top5_briefing_service 전용 pytest 작성. 현재 테스트 커버리지 0% (인라인 스모크만 존재).
8. **[🟢 Low]** 승인 카드 한국어 통일에 대한 사용자 피드백 1주일 수집 후 미세 조정.
9. **[🟢 Low]** 스모크 테스트 자동화 — pytest + httpx 로 CI 가능한 형태로.

## 9. GitHub 상태 (세션 6 종료 시점)

```
브랜치 (기준):   claude/verify-snapshot-push-F6oaZ
세션6 커밋:      c65442e fix(top5): cycle boundary off-by-one + fallback briefing policy
  변경 파일:     app/services/top5_briefing_service.py (643 → 691 lines)
  변경 내역:     Fix A (_current_cycle_date boundary) + Fix B' (select_top5 fallback, build_top5_briefing_text is_fallback, send_top5_briefing 2-pass)
이전 핵심 커밋:  1dcd581  fix(quality_scorer): 한국어 패턴 6종 추가
서버 워킹카피:   top5_briefing_service.py 만 세션6 버전 (git show 덮어쓰기)
서버 HEAD:       server-snapshot/20260410 (detached) ← git checkout 불가 (dirty worktree)
워킹트리:        dirty (18 modified + 7 untracked)
세션6 서버적용:  완료 (수동 검증 성공, §0.11). 05:00 KST 자연 발동 관측 대기중.
```

## 10. 통신 규칙 (운영자와 AI 간)
- 명령은 **합쳐서 한 번에** 제공 (쪼개지 말 것). 서버가 팅기면 2~4등분 허용.
- 스크립트는 heredoc으로 파일에 저장 후 `bash /tmp/xxx.sh` 실행.
- URL에 `http://` 문자열 금지 (조립 패턴 사용).
- rate_limiter sed 스왑은 **절대 금지** (세션 4 이후 KST-aware 로 수정됨, §4 A).
- 변경 후에는 실제 텔레그램 카드 스크린샷으로 최종 검증.
- 한국어 hook/body 텍스트는 **오직 대시보드 폼**에만 붙여넣기. 터미널에 붙여넣으면 `hook:: command not found` 남발.

---

**인수인계 끝. 질문 있으면 구체적 파일 경로·라인 번호와 함께 물어보세요.**
